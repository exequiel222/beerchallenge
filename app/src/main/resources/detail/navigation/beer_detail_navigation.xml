<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/movies_detail_navigation"
    app:startDestination="@id/movies_detail_entry">

    <fragment
        android:id="@+id/movies_detail_entry"
        android:name="com.exequiel.challenge.beers.features.detail.view.BeerDetailFragment"
        android:label="MovieDetailEntry"
        tools:layout="@layout/beer_detail_fragment">

        <argument
            android:name="movieId"
            app:argType="integer"
            android:defaultValue="0" />
    </fragment>

</navigation>